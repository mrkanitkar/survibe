import Foundation
import SVCore

/// Generates short video reels of practice sessions for sharing.
/// Full implementation in Phase 3.
public final class ReelGenerator: Sendable {
    public init() {}

    /// Generate a reel from a practice session recording.
    public func generate(from sessionURL: URL) async throws -> URL {
        // Phase 3: AVAssetExportSession composition
        sessionURL
    }
}
